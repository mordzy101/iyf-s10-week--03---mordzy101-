# Test Plan

## Manual Checks

- Confirm the project folders exist.
- Confirm `README.md` describes the project clearly.
- Confirm `.gitignore` includes common generated files and private files.
- Confirm `git log --oneline` shows at least five commits.
- Confirm the feature branch was merged into `main`.
